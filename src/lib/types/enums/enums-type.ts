export enum UserRole {
  USER = "user",
  TENANT = "tenant",
}

export enum PriceChangeType {
  NOMINAL = "nominal"
}

export enum PropertyCategory {
  VILLA = "villa",
  HOUSE = "house",
  APARTMENT = "apartment"
}