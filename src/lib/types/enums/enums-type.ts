export enum UserRole {
  USER = "user",
  TENANT = "tenant",
}

export enum PriceChangeType {
  NOMINAL = "nominal",
  PERCENTAGE = "percentage"
}

export enum PropertyCategory {
  VILLA = "villa",
  HOUSE = "house",
  APARTMENT = "apartment"
}